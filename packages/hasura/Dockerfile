FROM hasura/graphql-engine:v2.11.0-beta.1
RUN apt-get update \
  && apt-get install -y gnupg2 curl apt-transport-https

# COPY ./src/migrations /hasura-migrations
# COPY ./src/metadata /hasura-metadata
